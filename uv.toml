# Local workspace index config — not consumed by downstream dependents
# Switch between cu128/cu130 to match system CUDA version

[sources]
torch = { index = "pytorch-cuda" }
torchvision = { index = "pytorch-cuda" }

[[index]]
name = "pytorch-cuda"
url = "https://download.pytorch.org/whl/cu130"
explicit = true
